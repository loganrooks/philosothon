const FormEmbed = () => {
  // TODO: Replace with actual iframe embed code from Google Forms
  // Ensure the iframe is responsive and has appropriate height/width constraints
  return (
    <div className="bg-gray-100 p-8 rounded-lg min-h-[400px] flex items-center justify-center text-gray-500 border border-dashed border-gray-300">
      Google Form Embed Placeholder
    </div>
  );
};

export default FormEmbed;